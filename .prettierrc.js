module.exports = {
    semi: true,
    trailingComma: 'all',
    singleQuote: true,
    printWidth: 120,
    useTabs: false,
    tabWidth: 2,
    // 'auto' so a CRLF checkout on Windows does not turn every line into a lint error
    endOfLine: 'auto',
};
